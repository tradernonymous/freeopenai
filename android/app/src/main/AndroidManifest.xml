<?xml version="1.0" encoding="utf-8"?>
<!--
  FreeAI4U testing APK. Security posture, deliberately:
  - INTERNET is the only permission. No storage, contacts, camera, location.
  - No backup (adb or cloud) can lift the configured server URL or chats.
  - No WebView anywhere: there is no script surface to inject into.
  - Exactly one exported component (the launcher activity); every intent
    filter beyond MAIN/LAUNCHER would widen who can start what.
  - Cleartext is off here AND refused in code (Api.normalizeBaseUrl):
    chat content must never cross the network unencrypted.
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="false"
        android:fullBackupContent="false"
        android:icon="@drawable/ic_app"
        android:label="FreeAI4U"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreeAI4U"
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
